{
  base-dir = ${HOME}"/controller"
  rm2 = [{
    config = {
      type = "Rm2"
      name = "lounge"
      host = "192.168.1.109"
      mac = "34:EA:34:B5:2E:D2"
    }
    dependent-switch = {
      device = "hs100"
      name = "lounge"
    }
  }]
  stores {
    activity-store.location = ${base-dir}"/activity"
    macro-store.location = ${base-dir}"/macro"
    remote-command-store.location = ${base-dir}"/command"
    switch-state-store.location = ${base-dir}"/switch"
  }
  hs100.configs = [{
    name = "lounge"
    host = "192.168.1.103"
  }]
  config.file = ${base-dir}"/config"
  activity.dependent-switches {
    lounge = {
      device = "hs100"
      name = "lounge"
    }
  }
}